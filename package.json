{
  "name": "artifact-fetcher",
  "version": "1.0.0",
  "description": "A simple module to fetch RAILGUN artifacts",
  "main": "src/index.js",
  "scripts": {
    "lint": "eslint",
    "lint:fix": "eslint --fix",
    "build": "tsc --build",
    "clean": "tsc --build --clean",
    "test": "npm run build && node --test ./test/*.test.js",
    "test:only": "npm run build && node --test-only ./test/*.test.js"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/railgun-reloaded/artifact-fetcher.git"
  },
  "author": "RAILGUN Project Contributors",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/railgun-reloaded/artifact-fetcher/issues"
  },
  "directories": {
    "test": "test"
  },
  "homepage": "https://github.com/railgun-reloaded/artifact-fetcher#readme",
  "devDependencies": {
    "@railgun-reloaded/eslint-config": "^1.1.0",
    "@railgun-reloaded/tsconfig": "^1.2.0",
    "@types/node": "^22.13.10",
    "eslint": "^9.22.0",
    "typescript": "^5.8.2"
  }
}
